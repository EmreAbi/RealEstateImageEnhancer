# Cloudflare Pages Build Configuration

## Framework preset
- Framework: Vite

## Build settings
- Build command: `npm run build`
- Build output directory: `dist`
- Root directory: (leave empty or `/`)

## Environment variables
None required for this demo version

## Node version
- Node.js version: 18.x or higher (set in Cloudflare Pages settings if needed)

## Deployment branch
- Production branch: `main`
- Preview branches: All branches (optional)

## After deployment
The app will be available at: https://your-project-name.pages.dev

## Important Notes
1. Make sure the "Build output directory" is set to `dist`
2. Framework preset should be "Vite" (auto-detected)
3. Build command should be `npm run build`
4. Root directory should be empty or `/`
